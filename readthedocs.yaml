# readthedocs.yaml
version: 2

build:
  os: ubuntu-22.04
  image: latest

sphinx:
  configuration: docs/conf.py

python:
  version: 3.6
  install:
    - requirements: requirements.txt
    - method: pip
      path: .
