# readthedocs.yaml
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.6"
  apt_packages:
    - libclang
    - cmake

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: requirements.txt
    - method: pip
      path: .
